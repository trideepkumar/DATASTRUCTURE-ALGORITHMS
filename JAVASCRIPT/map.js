const orgArr  = [1,2,3,4,5]

const map = orgArr.map((element)=>{
     return element*2
})

console.log(map)

//creates a new array by performing function on each element of the original array

//does not change the original array
